#Configurations H2
spring:
    application:
      name: micorservice-paiement
    jpa:
      show-sql: true
    h2:
      console:
        enabled: true
  #défini l'encodage pour data.sql
    datasource:
      sql-script-encoding: UTF-8